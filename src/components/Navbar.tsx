import "twin.macro";

const NavBar = () => (
  <nav tw="flex justify-around bg-white p-4">
    <div>Home</div>
    <div>Receipts</div>
    <div>Profile</div>
    <div>Settings</div>
  </nav>
);

export default NavBar;
